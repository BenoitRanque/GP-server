import socketio from 'socket.io'
import { io } from './init'

io.sockets.on('connection', socket => {
  
})
io.sockets.on('connection', socket => {
  
})
io.sockets.on('connection', socket => {
  
})

export default io
